# Recommended Python version: 3.10 or 3.11
#
# This file covers the runtime needed to launch the main frontend/backend app:
# - backend API runtime
# - frontend proxy server runtime
# - embedding / retrieval runtime

--extra-index-url https://download.pytorch.org/whl/cpu

# API / server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-multipart>=0.0.9,<1.0

# Document processing / retrieval
torch==2.5.1+cpu
PyMuPDF>=1.24,<2.0
numpy>=1.26,<2.0
chromadb>=0.5,<0.7
sentence-transformers>=3.0,<4.0

# Tokenizer support for local model files
sentencepiece>=0.2,<1.0
